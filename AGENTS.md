# 🤖 Claude Code Agents 教程

> 使用 `/agents` 创建专属的 AI 代理，让不同任务由专门的代理处理

---

## 什么是 Agents？

Agents（代理）是**专门化的 Claude 实例**，每个代理有独立的：
- 角色定义
- 系统提示
- 允许的工具
- 使用的模型

---

## 查看现有 Agents

在 Claude Code 中输入：

```bash
/agents
```

你会看到：
- **Built-in agents** (内置代理): Bash, general-purpose, Explore, Plan 等
- **Plugin agents** (插件代理): 来自已安装插件
- **Custom agents** (自定义代理): 你创建的代理

---

## 创建自定义 Agent

### 方法 1: 通过 /agents 命令

```bash
/agents
→ 选择 "Create new agent"
→ 输入代理名称
→ 配置设置
```

### 方法 2: 创建 Markdown 文件

在项目目录创建 `.agent/agents/` 目录，添加 Markdown 文件：

```
.agent/
└── agents/
    └── r-analyst.md
```

**r-analyst.md 示例:**

```markdown
---
name: r-analyst
description: R 语言数据分析专家
model: sonnet
---

# R 数据分析专家

你是一个专业的 R 语言数据分析师，专注于流行病学和卫生统计。

## 核心职责

1. 编写高质量的 R 代码
2. 进行统计分析 (描述统计、回归、生存分析)
3. 生成出版级图表
4. 撰写分析报告

## 编程规范

- 禁止使用 for 循环，使用 purrr::map_*()
- 使用 tidyverse 风格
- 使用 ggsci 配色
- 双格式导出图片 (PNG + PDF)

## 工具权限

允许使用：
- 文件读写
- 执行 R 代码
- 生成图表
```

---

## 推荐的流统 Agents

### 1. 📊 R Analyst (R 分析师)

**用途**: 专门处理 R 数据分析任务

```markdown
---
name: r-analyst
description: R 语言数据分析专家
model: sonnet
---

专精于：
- 数据清洗与转换
- 统计建模
- 可视化
- 导出表格
```

### 2. 📝 Paper Writer (论文撰写)

**用途**: 根据分析结果撰写学术论文

```markdown
---
name: paper-writer
description: 学术论文撰写专家
model: opus
---

专精于：
- 论文结构组织
- 学术语言表达
- 结果解读
- 字数控制 (6000字以上)
```

### 3. 🔍 Code Reviewer (代码审查)

**用途**: 检查 R 代码质量

```markdown
---
name: code-reviewer  
description: R 代码审查专家
model: haiku
---

检查项：
- 代码规范
- 潜在错误
- 性能优化
- 可读性
```

### 4. 📚 Literature Helper (文献助手)

**用途**: 辅助文献检索和整理

```markdown
---
name: lit-helper
description: 文献检索与整理助手
model: haiku
---

功能：
- 检索 PubMed
- 整理引用格式
- 提取关键信息
```

---

## 调用 Agent

创建后，可以通过以下方式调用：

```bash
# 方法 1: /agents 菜单选择
/agents
→ 选择你的 agent

# 方法 2: @ 提及
@r-analyst 帮我分析这个数据

# 方法 3: 子代理调用 (在 Skills 中)
使用 r-analyst 代理来完成分析
```

---

## Subagents (子代理)

Subagents 是**被主代理调用的辅助代理**，用于：
- 并行处理任务
- 分担复杂工作
- 专门化处理

**创建 Subagent:**

```markdown
---
name: table-generator
description: 专门生成 gtsummary 表格
model: haiku
---

你只负责生成统计表格，使用 gtsummary 包。
完成后返回表格代码。
```

---

## 与 Skills 配合

Agents 和 Skills 可以协同工作：

| 组件 | 作用 |
| ---- | ---- |
| **Skills** | 提供知识和模板 |
| **Agents** | 定义角色和行为 |

**示例流程:**
1. 用户说 "分析这个数据"
2. 触发 `r-biostats` Skill (加载分析模板)
3. 使用 `r-analyst` Agent (执行分析)

---

## 模型选择建议

| 任务类型 | 推荐模型 |
| -------- | -------- |
| 复杂分析、论文写作 | opus |
| 常规编码、一般任务 | sonnet |
| 快速响应、简单任务 | haiku |
| 继承主会话模型 | inherit |

---

## 常见问题

### Q: Agent 和 Skill 有什么区别？

| | Skill | Agent |
|----|-------|-------|
| 作用 | 知识库 | 执行者 |
| 触发 | 自动 | 手动选择 |
| 内容 | 模板、规范 | 角色、行为 |

### Q: 如何让 Agent 使用特定 Skill？

在 Agent 的 markdown 中引用：

```markdown
开始工作前，先读取 r-biostats 技能的相关 reference 文件。
```

---

## 更多资源

- [Claude Agent SDK 文档](https://docs.anthropic.com/en/docs/claude-agent-sdk)
- [Claude Code 官方文档](https://docs.anthropic.com/en/docs/claude-code)
