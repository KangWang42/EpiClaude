
### 欢迎关注R语言数据分析指南
>本节来介绍一款R包**ggtrendline**可以向图形添加线性或非线性回归模型的趋势线及置信区间包含众多的模型，并可简单地向**ggplot**添加方程R 平方和P值。具体内容请参考作者官方文档

>line2P y = a*x + b;  
line3P, y = a*x^2 + b*x + c;    
log2P , y = a*ln(x) + b;  
exp2P, y = a*exp(b*x);  
exp3P, y = a*exp(b*x) + c;  
power2P, y = a*x^b;  
power3P, y = a*x^b + c.

### 官方文档
>https://github.com/PhDMeiwp/ggtrendline

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询，**添加小编微信请备注，以提高沟通效率**

### 安装R包
```r
# devtools::install_github("PhDMeiwp/ggtrendline@master", force = TRUE)
library(ggtrendline)
library(tidyverse)
```
### 案例 1
```r
x <- c(1, 3, 6, 9,  13,   17)
y <- c(5, 8, 11, 13, 13.2, 13.5)
```
```r
ggtrendline(x, y, model = "line2P")  
```

![](https://files.mdnice.com/user/7338/5598e10f-2903-4fe0-bf77-21a8ed6105f2.png)
### 案例 2
```r
ggtrendline(x, y, model = "line3P") + geom_point(aes(x, y)) + theme_bw()
```

![](https://files.mdnice.com/user/7338/4a2c2b3b-de87-4e9f-b2aa-d3a2bd055c99.png)

### 案例 3
```r
ggtrendline(x, y, model = "log2P", CI.fill = NA) + 不展示置信区间填充色
  geom_point(aes(x, y))+ theme_classic() 
```

![](https://files.mdnice.com/user/7338/cca4f22b-776b-40a1-ab10-4550a343706c.png)

### 案例 4
```r
ggtrendline(x, y, model = "exp2P", linecolor = "blue", linetype = 1, 
            linewidth = 1) + 
  geom_point(aes(x, y), color = "blue", shape = 1, size = 3)  
```

![](https://files.mdnice.com/user/7338/8b427715-53ca-425b-b200-9139e007a135.png)
### 案例 5 自定义置信区间
```r
ggtrendline(x, y, model = "exp3P", CI.level = 0.99, 
            CI.fill = "red", CI.alpha = 0.1, CI.color = NA, CI.lty = 2, CI.lwd = 1.5) + 
  geom_point(aes(x, y)) 
```

![](https://files.mdnice.com/user/7338/787dab38-aa63-44a3-9f63-8faec06aeb29.png)

### 案例 6
```r
data("iris")
x <- iris$Petal.Width
y <- iris$Petal.Length
group <- iris$Species
ggtrendline(x,y,"exp3P") + geom_point(aes(x,y,color=group))
```

![](https://files.mdnice.com/user/7338/6230f046-a0e1-415b-a0b3-acaf682b5732.png)

### 