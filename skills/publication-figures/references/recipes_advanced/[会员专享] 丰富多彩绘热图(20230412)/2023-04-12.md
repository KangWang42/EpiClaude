### 欢迎关注R语言数据分析指南

>还在绘制常规的热图吗，本节来介绍如何自定义绘制非常规热图，使你的热图更加丰富多彩。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载**

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询，**添加小编微信请备注，以提高沟通效率**

### 加载所需的R包
```r
library(tidyverse)
library(ggimage)
library(scales)
library(magick)
```
### 读取数据
```r
df <- readr::read_csv('data.txt')
```
### 读取图片进行修建
```r
egg <- magick::image_read("egg.svg") %>% magick::image_trim()
```
### 将图片转换为PNG格式
```r
egg_img <- magick::image_write(egg, path = "egg.img", format = "png")
```

![](https://files.mdnice.com/user/7338/5dd058af-8549-4f44-b251-777c77524d50.png)

### 数据清洗
```r
df <- df %>%
  filter(grepl("cage", prod_process)) %>%
  filter(prod_process == "cage-free (non-organic)") %>%
  arrange(observed_month) %>%
  mutate(
    mom = (n_eggs - lag(n_eggs)) / lag(n_eggs),
    label = percent(mom %>% round(2)),
    month = format(observed_month, "%m"),
    year = format(observed_month, "%Y")
  )
```
### 数据可视化
```r
df %>% ggplot(aes(x = month, y = year)) +
  geom_tile(color = "grey80", fill = "white", size = 0.5) +
  geom_image(aes(image = egg_img), size = 0.15, by = "height", asp = 1.5, hjust = 0.5) +
  geom_text(aes(label = label, color = ifelse(mom < 0, "#205FA7", "red")), size = 3.5, hjust = 0.5) +
  scale_color_identity() +
  scale_size_continuous(range = c(0, 30)) +
  scale_x_discrete(label = month.abb) +
  scale_y_discrete() +
  theme_void() +
  theme(
    legend.position = "none",
    axis.text.x = element_text(size = 8, color = "#000000", margin = margin(t = -5)),
    axis.text.y = element_text(size = 8, color = "#000000", margin = margin(r = 2)))
```