### 欢迎关注R语言数据分析指南

>本节来介绍如何使用ggplot2对表格之间进行数据的配对连线，发现有一款R包**bstfun**可以将gt绘制的表格转化为ggplot格式，通过其来绘制表格配对连线图则是非常的方便。下面小编就通过一个案例来进行展示数据为随意构建无实际意义仅作图形展示用，希望各位观众老爷能够喜欢。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载** R包安装可能比较麻烦建议下载到本地进行安装


### 关注下方公众号下回更新不迷路

1


### 图形展示
![](https://files.mdnice.com/user/7338/54811145-5976-4bd2-a254-c9ce19494187.png)

>此图可以看做三张图拼接而成，通过折线图进行连接；同时折线还可以展示多年份的数据，使得呈现的内容更加丰富。


### 加载R包
```r
library(ggplot2)
# install.packages("dplyr")
library(dplyr)
# remotes::install_local("bstfun-main.zip",upgrade = F,dependencies = T)
library(bstfun) 
library(patchwork) 
library(gt)
library(scales) 
library(tidyverse)
library(RColorBrewer)
```
### 数据整理
```
df <- read_tsv("data.xls") %>%
  group_by(year, industry) %>% # 按照 "year" 和 "industry" 列进行分组，.groups 参数设为 "drop_last" 以确保在后续操作中取消分组
  # 对每个分组计算 "rq" 列的中位数，并用 "med_rq" 列存储结果
  summarise(med_rq = median(rq), .groups = "drop_last") %>%
  # 按照 "med_rq" 列进行降序排列，.by_group 参数设为 TRUE 以确保在每个分组内进行排列
  arrange(desc(med_rq), .by_group = TRUE) %>%
  # 添加一个名为 "rank" 的新列，其值为 1 到 n() 的连续整数，表示每个分组内的排名
  mutate(rank = 1:n()) %>%
  # 取消分组，确保后续操作不再依赖先前的分组
  ungroup() %>%
  # 将 "rank" 列中的整数值转换为字符型，并用 "rank_fct" 列存储结果
  mutate(rank_fct=as.character(rank))

```
### 数据筛选
```r
# 从 df 数据框中筛选出 year 列等于 2022 的所有行，存储为新的数据框 df2022
df2022 <- df %>% filter(year == 2022)

# 从 df 数据框中筛选出 year 列等于 2017 的所有行，并将结果与一个新的数据框合并
# 新数据框包含一行数据：year 列为 2017，industry 列为 "Healthcare"，med_rq 列为 NA（缺失值），rank 列为 18
df2017 <- df %>% filter(year == 2017) %>% 
  bind_rows(tibble(year=2017, industry = "Healthcare", med_rq = NA_real_, rank = 18))

```

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员文档**同步更新中**售价149元**，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询。

### 淘宝扫一扫

![](https://files.mdnice.com/user/7338/51640c2b-89a0-4e84-b7a6-34d3077e9cb6.png)


### 2023会员群精彩内容

![1](https://files.mdnice.com/user/7338/5c8d56e1-ba77-45f8-899d-5176b7231d2e.png)
![2](https://files.mdnice.com/user/7338/392ddaf1-ac11-4477-86c5-32e3ddc88823.png)
![3](https://files.mdnice.com/user/7338/0ac4cdcd-17d1-4594-84c6-adcbafeb60cf.png)
![4](https://files.mdnice.com/user/7338/ac5cb532-2881-44ca-82a8-e6aaedba73cd.png)
![5](https://files.mdnice.com/user/7338/58a277f7-1c3b-4972-a815-3d0235af45dc.png)
![6](https://files.mdnice.com/user/7338/bc4e1dfc-c800-4cb8-8943-6b2762f169ca.png)


### 绘制线图
```
lines <- ggplot() +
  geom_line(data = df, aes(x = year, y = rank, group = industry))+
  geom_point(data = df %>% filter(year == 2022 | (year == 2017 & rank != 18)),
             aes(x = year, y = rank, fill = rank_fct),
             shape = 21, colour = "black", size = 3, stroke = 1)+
  scale_fill_manual(values=(colorRampPalette(brewer.pal(12, "Paired"))(18)))+
  scale_size_manual(values = c(0.25, 1)) +
  scale_alpha_manual(values = c(0.5, 1)) +
  scale_y_reverse(breaks = 1:max(df$rank), limits = c(NA, 0)) +
  scale_x_continuous(expand = c(0, 0)) +
  coord_cartesian(clip = "off") +
  theme_classic() +
  theme(legend.position = "none",
        axis.line.y = element_blank(),
        axis.ticks.y = element_blank(),
        axis.text.y = element_blank(),
        axis.text.x=element_text(color="black"),
        axis.title = element_blank(),
        plot.margin = margin(r = 5, t = 0, l = 5, b = 0))
```
### gt绘制表格
```r
# 使用 scales 包中的 col_numeric 函数创建一个颜色调色板，用于设置数据列 med_rq 的颜色
# 调色板中使用四种颜色："#004714"、"#35669D"、"#93A198"、"#FEF2F2"，alpha 参数设置透明度为 0.75
gt_palette <- scales::col_numeric(c("#004714", "#35669D", "#93A198", "#FEF2F2"),
                                  domain = NULL, alpha = 0.75)

# 从 df2022 数据框中选择 med_rq 和 industry 列，然后使用 gt 函数创建一个表格对象 tbl_2022
tbl_2022 <- df2022 %>% select(med_rq, industry) %>%
  gt() %>% 
  # 设置 med_rq 列的列标签为空字符串
  cols_label(med_rq = "") %>% 
  # 对 med_rq 列进行格式化为数字，并设置有效数字位数为 3
  fmt_number(columns = c(med_rq), n_sigfig = 3) %>% 
  # 对列标签进行样式设置，设置底部边框的粗细为 3 像素，单元格文本的字体加粗
  tab_style(locations = cells_column_labels(columns = everything()),
            style = list(cell_borders(sides = "bottom", weight = px(3)), cell_text(weight = "bold"))) %>% 
  # 对标题进行样式设置，设置文本的字体加粗，字体大小为 24 号
  tab_style(locations = cells_title(groups = "title"),
            style = list(cell_text(weight = "bold", size = 24))) %>% 
  # 对 med_rq 列进行数据颜色设置，使用之前创建的颜色调色板 gt_palette
  data_color(columns = c(med_rq), colors = gt_palette) %>% 
  # 将所有文本转换为大写字母形式
  opt_all_caps() %>% 
  # 设置 med_rq 列的文本对齐方式为左对齐
  cols_align(align = "left", columns = c(med_rq)) %>% 
  # 设置 industry 列的文本对齐方式为居中
  cols_align(align = "center", columns = c(industry)) %>% 
  # 设置 industry 列的宽度为 145 像素，med_rq 列的宽度为 70 像素
  cols_width(c(industry) ~ px(145), c(med_rq) ~ px(70)) %>% 
  # 设置表格的一些选项，包括顶部和底部边框颜色透明、数据行的内边距为 3 像素、source_notes 字体大小为 12 号、表头左对齐
  tab_options(
    column_labels.border.top.width = px(3),
    column_labels.border.top.color = "transparent",
    table.border.top.color = "transparent",
    table.border.bottom.color = "transparent",
    data_row.padding = px(3),
    source_notes.font.size = 12,
    heading.align = "left")

    
tbl_2017 <- df2017 %>% select(industry, med_rq) %>% 
  gt() %>% cols_label(med_rq = "") %>% 
  fmt_number(columns = c(med_rq), n_sigfig = 3) %>% 
  tab_style(locations = cells_column_labels(columns = everything()),
            style = list(cell_borders(sides = "bottom", weight = px(3)),
                         cell_text(weight = "bold"))) %>% 
  tab_style(locations = cells_title(groups = "title"),
            style = list(cell_text(weight = "bold", size = 24))) %>% 
  data_color(columns = c(med_rq),colors = gt_palette) %>%
  opt_all_caps() %>%
  cols_align(align = "right",columns = c(med_rq)) %>%
  cols_align(align = "center",columns = c(industry)) %>% 
  cols_width(c(industry) ~ px(145),c(med_rq) ~ px(70)) %>% 
  tab_options(
    column_labels.border.top.width = px(3),
    column_labels.border.top.color = "transparent",
    table.border.top.color = "transparent",
    table.border.bottom.color = "transparent",
    data_row.padding = px(3),
    source_notes.font.size = 12,
    heading.align = "left")
```
### 将表格转化为gg格式
```r
tbl_2017_ggplot <- bstfun::as_ggplot(tbl_2017)
tbl_2022_ggplot <- bstfun::as_ggplot(tbl_2022) 
```
### 拼图
```
tbl_2017_ggplot + lines + tbl_2022_ggplot
```

![](https://files.mdnice.com/user/7338/d0cd6414-0b8d-4b53-8d7d-fcd4be094f9b.png)



