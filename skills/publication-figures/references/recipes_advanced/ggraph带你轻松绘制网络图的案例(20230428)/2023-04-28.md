>最近VIP群里有朋友提问网络图的绘制方法，小编之前写过了不少的案例，但是由于图形布局一变可能大家找不到完全契合的代码来使用，今天在根据要求写一个网络图的绘制案例。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载**

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询.**添加小编微信请备注，以提高沟通效率**

### 淘宝店铺个性化绘图服务
>有需要找小编做个性化绘图的朋友欢关注**淘宝店铺(R语言数据分析指南)** 有需求者可咨询下单。


### 加载R包
```r
library(tidyverse)
library(igraph)
library(ggraph)
library(widyr)
```
### 导入数据
```r
loadouts <- readr::read_csv('loadouts.txt')
```
### 数据清洗
```r
df <- loadouts %>%
  pairwise_count(item, name, sort = TRUE, upper = FALSE) %>%   # 计算配备物品之间的成对计数
  graph_from_data_frame()
```
### 数据可视化
```r
df %>% ggraph(layout = "fr") +   # 构建图形布局，若需要圆形将其改为circle
# 添加边，设置透明度、宽度和颜色
  geom_edge_link(aes(edge_alpha = n, edge_width = n), edge_colour = "#205FA7") + 
  # 添加节点，设置大小、形状和颜色
  geom_node_point(size = 4, shape = 21, color = 'grey20') +
  # 添加节点文本，设置自动避让和字体大小
  geom_node_text(aes(label = name), repel = TRUE, size = 3) +
  theme_void() +   # 设置背景为透明
  # 设置主题，包括文本大小、颜色、背景颜色等
  theme(text = element_text(size = 10, color = 'grey20'),
        plot.background = element_rect(fill = '#edede9'),
        legend.position = 'none',
        panel.grid = element_blank(),
        plot.margin = margin(5, 5, 5, 5))
```

![](https://files.mdnice.com/user/7338/0f68dfac-18ff-477b-9c40-7db789add68f.png)

![](https://files.mdnice.com/user/7338/f14f3a12-daad-49f4-b3e4-a4ce1de7a06b.png)

>可以看到非常的简单，好了今天的介绍到此结束；喜欢的观众老爷欢迎分享转发，有学习需求者欢迎到小编**淘宝店铺 R语言数据分析指南** 购买小编2023年的VIP文档，已有不少朋友学以至用用到了论文当中，**加小编微信请备注信息及来意，无需要请勿扰
