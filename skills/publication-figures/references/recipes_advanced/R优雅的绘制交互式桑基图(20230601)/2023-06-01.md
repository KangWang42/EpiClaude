### 欢迎关注R语言数据分析指南

>最近VIP交流群中有朋友询问一张桑基图的画法，目前在R中绘制桑基图的包主要使用**ggsankey**来实现，但是此图由于是交互式的因此可见是由**networkD3**包绘制而成，下面就来通过一个小案例介绍如何使用**networkD3**包来绘制一个交互式的桑基图。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载** 


>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询。



### 加载R包
```r
library(tidyverse)
install.packages("networkD3")
library(networkD3)

```
### 导入数据
```r
refresults <- read.csv("result-data.csv")
```
### 数据清洗
```r
# 按地区分组进行计算
results <- refresults %>%
  group_by(Region) %>%
  summarise(Remain = sum(Remain), Leave = sum(Leave)) %>%
  pivot_longer(-Region, names_to = "result", values_to = "vote")
```
### 构建节点数据
```r
regions <- unique(results$Region)
nodes <- data.frame(node = 0:13, name = c(regions, "Leave", "Remain"))
```
#### 合并结果和节点数据
```r
results <- results %>%
  inner_join(nodes, by = c("Region" = "name")) %>%
  inner_join(nodes, by = c("result" = "name")) %>%
  rename(source = node.x, target = node.y, value = vote)
```
### 创建链接数据
```r
links <- results[, c("source", "target", "value")]
```
### 绘制交互式桑基图
```r
sankeyNetwork(Links = links, Nodes = nodes, Source = "source", 
              Target = "target", Value = "value", NodeID = "name",
              units = "votes")
```

![](https://files.mdnice.com/user/7338/dfc4b7fb-b504-41d6-9596-3eb0b6365b81.png)




