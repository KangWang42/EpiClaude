>本节来通过**ungeviz**包介绍如何对各大洲人类发展指数变化趋势进行分析与可视化，主要分析了1990年和2021年各大洲的人类发展指数（HDI）边际平均值的变化趋势，并通过置信区间密度图可视化地展示了这些变化及其不确定性。**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载**

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询。

### 淘宝店铺个性化绘图服务
>有需要找小编做个性化绘图的朋友欢关注**淘宝店铺(R语言数据分析指南)** 有需求者可咨询下单。



### 加载R包
```r
# ungeviz：提供用于可视化不确定性的统计图层
# broom：将统计分析结果转换为整洁格式的工具
# emmeans：计算线性模型的边际平均值
library(tidyverse)
# devtools::install_github("wilkelab/ungeviz")
library(ungeviz)
library(broom)
library(emmeans)

### 导入数据
```r
# 从文件中读取人类发展指数（HDI）数据和人口数据
hdi <- read_tsv("hdi.xls")
population <- read_tsv("population.xls")
```
### 结果展示

![](https://files.mdnice.com/user/7338/d60de59c-6bdb-476e-8d51-9bcd151d84d5.png)


### 线性回归分析
```
# 使用1990年的数据进行线性回归分析，以大洲为自变量，HDI为因变量
# 对各大洲的HDI进行边际平均值计算，并整理结果
# 将大洲变量转换为因子类型，并按预定顺序重新排序
# 添加年份变量，设为1990
hdi_old <-
  lm(hdi ~ continent, data = filter(hdi, year == min(year)),
     weights = population) %>% emmeans("continent") %>%
  tidy() %>%
  mutate(continent = factor(continent),
         continent = fct_reorder(continent, 1:5),
         year = 1990)
         
# 使用2021年的数据进行线性回归分析，以大洲为自变量，HDI为因变量
# 对各大洲的HDI进行边际平均值计算，并整理结果
# 将大洲变量转换为因子类型，并按预定顺序重新排序
# 添加年份变量，设为2021
hdi_new <-
  lm(hdi ~ continent, data = filter(hdi, year == max(year)),
     weights = population) %>%
  emmeans("continent") %>% tidy() %>%
  mutate(continent = factor(continent),
         continent = fct_reorder(continent, 1:5), year = 2021)
```
### 数据合并
```r
plot_data <- bind_rows(hdi_new, hdi_old) %>%
  mutate(year = factor(year))
```
### 定义颜色
```r
txt <- "#0076BE"
cornflower <- "#95D8EB"
oceangreen <- "#48BF91"
bg <- "grey95"
```
### 数据可视化
```r
# 使用ggplot绘制图形，展示1990年和2021年各大洲的HDI边际平均值及其不确定性
ggplot(plot_data, aes(x = estimate, moe = std.error, y = year)) +
  stat_confidence_density(aes(group = year, fill = year),
                          height = 0.6, confidence = 0.68) + # 绘制置信区间密度图
  geom_point(aes(x = estimate), size = 3) + # 绘制散点图
  geom_errorbarh(aes(xmin = estimate - std.error,
                     xmax = estimate + std.error),
                 height = 0.3) + # 绘制水平误差条
  scale_fill_manual(values = c(oceangreen, txt)) +
  xlim(0.3, 1.0) +
  facet_wrap( ~ continent ~ ., ncol = 1, switch = "y") +
  labs(y = NULL) +
  theme_minimal() +
  theme(
    text = element_text(family = "font", colour = txt,size = 10),
    plot.background = element_rect(fill = bg,colour = bg),
    axis.text = element_text(colour = txt),
    panel.grid.major = element_line(colour = "grey70"),
    panel.grid.minor.x = element_blank(),
    strip.placement = "outside",
    strip.text.y.left = element_text(size = 10,colour = txt,face = "bold",angle = 0,),
    panel.spacing = unit(2, "lines"),plot.margin = margin(5,5,5,5))
```



