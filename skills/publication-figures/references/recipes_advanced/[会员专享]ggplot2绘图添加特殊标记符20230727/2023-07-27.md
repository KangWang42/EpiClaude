### 欢迎关注R语言数据分析指南

>本节来介绍如何使用ggplot2绘图时使用**showtext**添加特殊字符集合，下面小编就通过一个案例来进行展示数据为随意构建无实际意义仅作图形展示用，添加了详细的注释希望各位观众老爷能够喜欢，**数据+注释代码已经上传2023VIP群，加群的观众老爷请自行下载**。

### 图形展示

![](https://files.mdnice.com/user/7338/b316b19b-05db-4fcf-a116-72bb104412cd.png)

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员文档**同步更新中**售价149元**，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询。

### 加载R包
```r
library(tidyverse)
library(ggtext)
library(showtext)
showtext_auto(enable = TRUE)
```

### 数据清洗
```r
scurvy2 <- read_tsv("data.xls") %>% 
  mutate(Frequency = str_replace(Frequency, "times", "times \n"))
```

### 数据可视化


>https://www.fileformat.info/info/unicode/char/search.htm （unicode字符查询)
```r
ggplot(scurvy2, aes(x = Patient, y = Parameter)) +
    geom_tile(data = scurvy2, aes(fill = Value, height = 0.80, width = 0.8)) +
    # 添加散点图层，数据源为scurvy2，筛选出Parameter为"Fit for duty?"，Value为"Yes"的数据
    # 使用"\u002B"作为散点形状，设置大小为8，颜色为"#A88AD2"
    geom_point(data = scurvy2 |> filter(Parameter == "Fit for duty?" & Value == "Yes"),
               shape = "\u002B", size = 8, color = "#A88AD2") +
    geom_point(data = scurvy2 |> filter(Parameter == "Fit for duty?" & Value == "No"),
               shape = "\u2716", size = 8, color = "#60627C") +
    # 添加文本标签，设置标签内容为"Frequency"，只在Patient为"1"时显示，y坐标为6，x坐标为1.5
    geom_text(aes(label = ifelse(Patient == "1", Frequency, NA), y = 6, x = 1.5), 
              size = 3, hjust = 0.5, check_overlap = TRUE,color = "black") +
    expand_limits(y = 6.5, x = c(-0.2, 1.5)) +     # 设置y轴上限为6.5，x轴范围为-0.2到1.5
    # 根据Regimen进行面板分割，每个面板的标签最多显示16个字符
    facet_grid(. ~ Regimen, labeller = label_wrap_gen(16)) +
    # 手动设置填充颜色映射表，分别为不同水平"None"、"Mild"、"Moderate"和"Severe"设置颜色
    # 将NA值和空单元格设置为透明
    scale_fill_manual(values = c("#788FCE","#E6956F","#A6BA96","#BD8184", NA, NA), 
                      na.value = NA, breaks = c("None", "Mild", "Moderate", "Severe")) + 
    theme_classic() +
    theme(axis.title = element_blank(), 
          axis.ticks.x = element_blank(), 
          axis.text.x = element_blank(),
          panel.spacing.x = unit(0,"cm"),
          strip.text = element_text(color="black", size=8, face="bold"),
          legend.background = element_rect(fill = "white", color = NA), 
          axis.text.y = element_text(size = 9, color = "black", face = "bold"),
          legend.text = element_text(size = 9, color = "black"), 
          legend.title = element_blank())
```

