### 欢迎关注R语言数据分析指南

>本节来分享iMeta一篇文章中的代码，ggtree绘制进化树；作者给出了可执行的完整代码小编对其进行了注释，**数据+代码可以直接去原文中下载**,小编整理后的**数据+代码已经上传2023VIP群，加群的观众老爷请自行下载**

### 原文
>Linking biodiversity and ecological function through extensive microeukaryotic movement across different habitats in six urban parks

>有需要学习个性化数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**购买**2023年度会员** `初始价格99元`，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；按照往年数据小编年产出约在150+以上

购买后微信发小编订单截图即邀请进新的会员交流群，小编的文档为按年售卖，只包含当年度的**除系列课程外**的文档，有需要往年文档的朋友也可下单购买，需要了解更多信息的朋友欢迎交流咨询，**添加小编微信请备注，以提高沟通效率**

### 加载所需的包
```r
library(vegan)
library(tidyverse)
library(ggtree)
library(treeio)
library(tidytree)
library(ggtreeExtra)
library(ggnewscale)
library(ggstar)
```
### 读取数据
```r
v4 <- read.table(file = "V4.txt", sep = '\t', header = T, row.names = 1)
g4 <- read.table(file = "group.v4.txt", sep = '\t', header = T)
v9 <- read.table(file = "V9.txt", sep = '\t', header = T, row.names = 1)
g9 <- read.table(file = "group.v9.txt", sep = '\t', header = T)
```
### 计算 V4 域的列和
```r
v4domain <- c()
v4n <- unique(v4$D)
for (i in 1:length(v4n)) {
  v4domain <- rbind(v4domain, colSums(v4[v4$D == v4n[i], 9:98]))
}
v4domain <- as.data.frame(v4domain)
rownames(v4domain) <- v4n
```
### 计算 V9 域的列和
```r
v9domain <- c()
v9n <- unique(v9$D)
for (i in 1:length(v9n)) {
  v9domain <- rbind(v9domain, colSums(v9[v9$D == v9n[i], 9:98]))
}
v9domain <- as.data.frame(v9domain)
rownames(v9domain) <- v9n
```
### 修改列名
```r
colnames(v4domain) <- paste(colnames(v4domain), ".v4", sep = "")
colnames(v9domain) <- paste(colnames(v9domain), ".v9", sep = "")
```
### 添加行名
```r
v4domain$n <- rownames(v4domain)
v9domain$n <- rownames(v9domain)
```
### 合并数据
```r
domain <- full_join(v4domain, v9domain, by = "n")
rownames(domain) <- domain$n
domain <- domain %>% select(-n)
rownames(domain)[4] <- "Unidentified"
```
### 计算 Bray-Curtis 距离
```r
d.domain <- vegdist(t(domain), method = "bray")

# 进行层次聚类
f.domain <- hclust(d.domain, method = "ward.D2")

# 绘制聚类树
plot(f.domain, hang = -1, main = "ward.D2", sub = "", xlab = "domain")
```

### 转换为进化树对象
```r
tree.domain <- as.phylo(f.domain)

# 为样本添加标签
g4$label <- paste(g4$sample, ".v4", sep = "")
g9$label <- paste(g9$sample, ".v9", sep = "")

# 添加样本类型
g4$l <- "v4"
g9$l <- "v9"

# 合并分组信息
g.all <- rbind(g4, g9)

# 交换列名
a <- g.all$label
g.all$label <- g.all$sample
g.all$sample <- a
names(g.all)[4] <- "lab"

# 按生境分组
groupInfo.domain <- split(g.all$sample, g.all$habitat)
```
### 对进化树中的 OTU 进行分组
```r
tree.domain.g <- groupOTU(tree.domain, groupInfo.domain)
```
### 自定义生境颜色
```r
habitat.col <- c("#339933", "#990000", "#CC9900", "#663366", "#99CCFF")
```
### ggtree 绘制进化树
```r
p.domain <- ggtree(tree.domain.g, layout = "fan", ladderize = T, size = 0.5,
                   branch.length = "none", aes(color = group)) +
  scale_color_manual(values = habitat.col) +
  theme(legend.position = "right") +
  new_scale_colour()

# 将分组信息添加到图中
p.domain <- p.domain %<+% g.all

# 定义公园颜色
park.col <- c("#8ECFC9", "#FFBE7A", "#FA7F6F", "#82B0D2", "#BEB8DC", "#E7DAD2")

# 向图中添加点
p.domain +
  geom_tippoint(mapping = aes(colour = l), size = 3, stroke = 0, alpha = 1) +
  scale_colour_manual(name = "Region", values = c("#9ac9db", "#f8ac8c"),
                      guide = guide_legend(keywidth = 0.3, keyheight = 0.3, ncol = 1,
                                           override.aes = list(size = 2, alpha = 1), order = 1)) +
  new_scale_colour() +
  # 添加星形表示公园
  geom_fruit(geom = geom_star,
             mapping = aes(fill = park), starshape = 1, colour = NA,
             size = 2, starstroke = 0, pwidth = 0.1, offset = 0.08,
             inherit.aes = FALSE) +
  # 定义公园颜色填充
  scale_fill_manual(name = "Park", values = park.col,
                    guide = guide_legend(keywidth = 0.3, keyheight = 0.3, order = 3),
                    na.translate = FALSE) +
  # 调整图例样式
  theme(legend.title = element_text(size = 8),
        legend.text = element_text(size = 6),
        legend.spacing.x = unit(0.1, "cm"),
        legend.spacing.y = unit(0.1, "cm"))
```

![](https://files.mdnice.com/user/7338/6ccdc3d8-2253-4c78-8a05-5059d6e65e23.png)

>可以看到处理过程内容还是很多的，此外这篇文章作者给出了全套完整的代码，实在是一份学习的好材料，喜欢的观众老爷欢迎分享转发。有学习数据可视化需求者欢迎到小编**淘宝店铺 R语言数据分析指南** 购买小编2023年的VIP文档，已有不少朋友学以至用用到了论文当中，**加小编微信请备注信息及来意，无需要请勿扰**

