---
name: research-visuals
description: 以 Codex 内置 imagegen/image_gen 为默认路径，为科研、医学、公共卫生和学术内容生成跨载体高质量视觉资产，并在实际不可用或精度不合格时回退 SVG。以下任务必须先触发本技能：(1) 流程图、技术路线、结构图、概念框架、机制示意、时间线、系统架构和图形摘要；(2) PPT 封面、主视觉、章节配图、研究背景和方法配图；(3) 论文或报告的概念插图、科学教育插图、封面和章节图；(4) 基金标书的立项依据、科学问题、创新点、研究内容、技术路线和预期影响配图；(5) 网页 hero、登录页侧栏、横幅、内容配图、功能卡片、空状态和定制插画；(6) 现有非统计视觉资产重绘。文字或数字密集不得预先改走 SVG。统计数据图走 publication-figures；真实科研原始图像不得生成式重绘；已有 UI 图标系统优先复用；显式要求矢量、工具实际不可用或生成后精度仍不合格时才转 svg-diagrams。
---

# Imagegen 科研视觉工作流

把 imagegen 作为科研非统计视觉资产的默认生成路径，而不是固定画风生成器。先判断载体、读者、信息功能、证据属性和实际显示尺寸，再选择主体、视觉语言、材质、构图、配色与细节密度。科研感必须来自真实主题和关系，不来自 DNA、显微镜、试管、冷蓝界面或公式堆砌。

Codex 使用内置 `imagegen` 技能及其 `image_gen` 工具。不要另写 API 脚本，不猜模型参数，不把 CLI/API 当作内置工具失败后的静默替代。其它平台只使用当前会话明确提供且已配置的图像生成工具。

## 必读资源

- 每次生成前读 `references/visual-strategy.md`，确定视觉角色、风格、构图和跨资产一致性。
- 按实际载体读 `references/carrier-specs.md` 的 PPT、论文、标书、报告或网页章节。
- 输入是完整论文、项目材料、多图系列、复杂多面板或参考图时，先读 `references/figure-planning.md`，完成来源到图件矩阵、图前合同、参考图解构和四轮签发。
- 生成流程图、框架图、机制图、研究设计、技术路线或图形摘要前，完整阅读 `references/research-figure-patterns.md`，锁定图类的内容契约、证据属性和视觉语法。
- 流程图、研究设计、技术路线、方法架构或图形摘要需要决定是否使用 icon 时，读取 `references/diagram-iconography.md`；先判定不用图标是否更清楚，再确定数量、复杂度、主题映射、统一风格和配色。
- 内容契约锁定后读 `references/prompt-recipes.md`，选择对应提示词配方。
- 计算模型、网络架构、复杂多面板或需要扩展提示词词汇时，先读 `references/external/SOURCE.md` 的调用规则，再按需检索所列上游原文；不得把上游文件当独立 skill 直接执行。
- 同时遵循内置 `imagegen` 的生成、编辑、保存路径、目标图选择和透明背景规则；全新创作走纯文本生成，修改既有图或修正上一版走携图编辑。

## 证据属性检查

先给目标视觉归类，不能为了“更美观”跨越证据边界：

1. **证据型原始图像**：显微、病理、影像、临床照片、仪器输出、原始截图、凝胶或其它研究证据，不得由 imagegen 新造、补全、净化或重绘。仅做期刊允许且不改变信息的确定性处理，并保留原始文件。
2. **统计数据图**：任何坐标、比例、效应值、置信区间、样本量或真实数据映射，调用 `publication-figures`，不得让 imagegen 虚构。
3. **精确结构图**：流程、机制、框架、路线、图形摘要先用 imagegen 生成包含完整文字、数字和箭头的整张图；逐字逐边核验，多轮仍不合格才整图回退 `svg-diagrams`。
4. **解释与编辑插图**：科学教育插图、研究场景、概念配图、封面、网页主视觉和标书配图默认用 imagegen。
5. **界面控制图标**：按钮、工具栏、导航和状态控件已有 Lucide 或项目图标系统时直接复用，不生成位图替代；定制功能插画、内容卡片和空状态可用 imagegen。

## 不降质合同

图片编辑和重绘必须把 Image 1 视为验收基线，而不是可覆盖的临时输入。提示词只能降低风险，不能保证候选图更好；原图始终保留，候选图只有通过全部验收条件后才成为当前版。

按以下优先级比较 Image 1 与候选图，前项不能用后项补偿：

1. 事实与语义忠实度。
2. 文字、数字、公式和人物或对象身份的精确性。
3. 对象、节点、箭头、分支、汇合和关系的正确性。
4. 最终显示尺寸下的可读性。
5. 视觉精修。

候选图更美观但任一高优先级项目变差时，判定失败并保留 Image 1。不得覆盖、删除或改名原图，直到候选图通过对应图类的全部 acceptance checks。

参考图角色必须显式锁定：

- **Image 1**：唯一编辑目标与验收基线，科学内容、身份和结构从此图继承。
- **Image 2**：仅在用户明确要求风格迁移时作为可选风格参考，只传递配色、间距节奏和线条处理；不得传递对象、标签、数据、错误布局或品牌。
- 不需要风格迁移时不附 Image 2；不得让 Image 2 覆盖 Image 1 的内容合同。

## 开始前检查

- 命中本技能 `description` 后，必须先加载本技能与所需 references，再选择实现路径。当前可见 skills 清单未列出本技能，不等于 `image_gen` 不可用，也不是转 SVG 的依据。
- PPT 任务先为每页指定主要表达方式。触发本技能不等于每页都要生成图片；目录、章节、目的、定义、公式、创新、可行性和计划页若用模板、文字、表格、公式或时间轴表达更清楚，应明确不生成装饰插图。
- 信息足够时直接执行。只有缺失项会明显改变内容、比例或留白方向时，最多提出一个必要问题；其余按载体和现有视觉系统作保守判断。
- 通用 skill 只保存可迁移的方法、字段和验收规则。项目专属的正式图号、导出文件名、标签、方法名、阈值、比例、配色和单次修正只进入本次运行时合同或项目记录，不写回本技能或 references。
- 用户要求实际生成图片、PPT、论文、标书、报告或网页时，不得停在方案、提示词或“正在运行”。必须完成生成、查看原图、修正、保存、嵌入和最终载体检查。

## 核心工作流

0. **规划图件集合**：完整论文、项目或多图请求先按 `figure-planning.md` 建立来源到图件矩阵。只保留来源明确、具有独立信息贡献且能在最终尺寸成立的候选；不按章节数量机械配图。单张明确请求执行缩略图前合同即可。
1. **读取载体环境**：检查模板、页面或网页的比例、占位、背景、配色、字体、相邻文字、按钮位置和已有图片。载体内嵌图先按用户指向、图题与正文交叉引用、页面或段落位置、文档关系和渲染外观定位；导出文件名和媒体序号仅作存储线索，不得从中推断正式图号。PPT 先按页型标记 `插图/统计图/原始证据/表格/公式/流程图/时间轴/纯文字`；没有现成视觉系统时才建立新的系统。
2. **锁定信息功能**：只选一个主要角色：解释关系、建立情境、提示功能、区分章节、突出创新、展示路径或形成品牌识别。一个图片不承担所有角色。
3. **形成视觉简报**：在内部写不超过 120 字的规格，包含载体、读者、一句核心命题、来源锚点、主体、风格、构图、配色来源、材质、比例、负空间和禁止项。不得先套固定画风。
4. **锁定内容契约**：列出各区域的独立贡献，以及必须出现、必须逐字准确、可以抽象、不得出现和不得推断的对象、文字、数字与关系。来源没有的结论、期刊标识、仪器读数或医学细节不得补写；移除后不影响核心命题的区域直接删除。
5. **建立视觉系统**：同一项目的系列图共享主色来源、光线方向、材质、线条或摄影语言；不同载体按实际尺寸调整密度和裁切，不能把同一模板机械复制到所有图片。流程图默认使用两类有明确含义的主色相，只有真实警示、失败、异常、不良结局或必须单独识别的关键状态才增加第三类；全图主色相不超过三种，黑、白、灰等中性色不计入。不得把蓝白配色作为科研流程的默认答案，也不得为了避免单调而随机换色。流程与路线图同时锁定 icon 策略：默认无图标，只有真实对象、操作或产出需要视觉锚点时才加入少量简单 icon 或微型图元。
6. **构造提示词**：使用 `figure-planning.md` 的结构化提示词包和 `prompt-recipes.md` 的对应配方，至少保留来源锚点、核心命题、证据属性、结构原型、区域、语义微型图元、icon 策略、精确节点与边、逐字标签、配色角色、安全区和禁止推断项。用具体视觉语法替代“高级、科技感、学术感”等空泛词，不设机械字数下限。
7. **生成首稿**：首轮只生成一个完整方案。全新生成不携带目标图；非编辑目标的参考图只用于提取拓扑、构图、视觉权重和配色角色并写进完整提示词。项目产物必须移入工作区，不得只留在 `$CODEX_HOME/generated_images/`。
8. **双尺度检查**：先按原始分辨率检查伪文字、解剖、纹理、阴影和重复对象，再按最终显示尺寸检查主旨、轮廓、层级、负空间、对比、裁切和细节密度。
9. **携图定向编辑**：需要修改既有图、根据既有图重绘或修正上一版时，先查看 Image 1 并写三段式合同：`LOCKED` 列出不得改变的文字、数字、公式、身份、对象、节点、边、方向和结论；`FLEXIBLE` 列出允许优化的版式、间距、字体层级、配色、线宽、背景和视觉重心；`FORBIDDEN` 列出不得添加、删除、纠正、推断或合并的内容。所有待附图片均有本地路径时使用 `referenced_image_paths`；近期对话能覆盖全部待附图片时使用最小 `num_last_images_to_include`；两者不得并用。单一机制无法覆盖 Image 1 与必要 Image 2 时，省略非必要 Image 2；风格迁移确属必要时要求重新附图。每轮只修一个主要问题并重复三段式合同；成功返回的候选图必须与 Image 1 全幅对照并通过不降质合同。最多连续两轮候选图质量修正；仍失败时，仅对可替代的生成稿执行一次纯文本整图重生成。精确结构图仍不准确时整图回退 `svg-diagrams`；不可替代的用户原图保留原件，不以重生图冒充修改成功。
10. **嵌入与载体复核**：把最终图放进实际 PPT、论文页面、标书页或网页；渲染/截图检查整体，而不是只查看孤立图片。网页同时检查桌面与移动端，PPT 检查缩略图和投影尺寸。
11. **记录与归档**：正式项目记录最终视觉简报、提示词、工具路径、核验结论和回退原因；当前版使用稳定文件名，旧版整组归档。提示词和生成过程不进入正文。

签发按 `figure-planning.md` 依次通过四轮：必要性与证据、内容与拓扑、视觉与可读性、最终载体。所有 must-fix 闭环后才能交付；参考图或外部示例不能替代来源核验。

## HTTP 524

把 HTTP 524 视为携图生成的服务或传输失败，不是候选图质量失败，也不计入两轮设计修正：

1. 第一次 524：使用相同 Image 1、相同必要附件和同一修改目标重试一次；压缩重复形容词和通用禁止项，但保留图片角色、结构 inventory、`LOCKED / FLEXIBLE / FORBIDDEN` 与 acceptance checks。
2. 第二次 524：停止重试，保留 Image 1，不解释为提示词或设计失败；不静默降级模型，不另写或切换第三方 API，也不因超时回退 SVG。

只有 imagegen 成功返回候选图且候选图不满足内容精度要求，才进入后续重生或 SVG 回退判断。

## 美学要求

- 主体必须一眼可辨。小图保留一个主对象，大图通常保留一个焦点和 2 至 4 个支持元素。
- 优先自然不对称构图、稳定视觉重心和有用途的负空间；避免平均分布、机械居中、边缘拥挤和无意义漂浮。
- 配色优先取自现有载体。流程图默认使用两类语义主色，必要时增加一类警示或关键状态色，主色相总数不超过三种；中性色用于画布、文字、边框和连接线。红色只用于真实警示、失败、异常、不良结局或当前重点，普通排除、常规步骤和一般分组不自动标红。不把整个项目做成单一蓝色相，也不为“高级感”加入霓虹渐变。
- 材质必须有语义：文献与证据可用纸张、油墨和索引；临床与现场可用自然光纪实；机制与系统可用干净科学插画；数字研究可用克制几何和网格。不得无理由加入玻璃、金属、烟雾、景深或电影灯光。
- 不默认使用塑料 3D 图标、黏土风、儿童卡通、商业素材照片感、悬浮全息屏、蓝色发光 DNA、无依据分子结构或科研符号拼贴。
- 系列资产要有家族一致性，但每张图仍服从其内容角色，不能变成同构卡片墙。

## 文字与关系

- PPT 主视觉、封面、网页 hero、登录页和章节配图默认不烧录标题、正文、按钮或长解释；这些文字由载体原生排版承担。
- 流程图、机制图、技术路线和图形摘要需要图内标签时，必须由 imagegen 直接生成完整图并逐字核验。不得用 Python、PPT 文本框、Word 文本框或 SVG 覆盖层修补错误文字、数字和箭头。
- 图内标签逐字沿用来源材料、研究方案或可核验方法文献中的规范术语。缩短标签只能删除不影响含义的修饰语，并保留术语核心；不得加入内部流程词、软件工程隐喻、口语化动作、生硬直译、自造缩略语或临时拼接的短语。来源未给出且存在多种合理名称时先向用户核实。
- 图题、图号、论文标题、报告标题和来源说明通常留在载体中，不烧录进图片，除非目标格式明确要求。
- 生成似真论文页面、仪器界面或档案时，只使用不可辨识的短线、点阵和抽象标记，不生成看似真实但错误的段落与数值。
- 修改既有图片、根据既有图重绘和修正上一版成图都属于携图编辑。只传全部且仅必要的编辑目标；上一轮生成结果需要继续修改时，以该结果作为新的编辑目标，同时用初始原图核对漂移。
- 非编辑目标的风格或布局参考默认不随调用上传，只把可迁移规则写入提示词；明确需要风格迁移时可增加一张 Image 2，但必须锁定其仅提供风格，不提供内容。用户明确要求同时修改多张目标图时才传入多张编辑目标。

## 路由与回退

- imagegen 可用时，流程图、框架图、示意图、图形摘要和跨载体配图不得因“SVG 更容易”而提前跳过。
- 用户明确要求 SVG、矢量源、完全可编辑图形或目标格式强制矢量时，调用 `svg-diagrams`。
- 已检查实际工具后确认不可用，或成功返回的候选图经携图编辑与允许的纯文本整图重生成仍不能满足精度要求时，精确结构图整图回退 `svg-diagrams`。HTTP 524 本身不是 SVG 回退凭证。
- 回退时沿用已锁定的内容契约和视觉简报，不在切换工具时改变信息结构。

## 强制自检

- [ ] 已判定证据属性；真实数据和科研原始图像未被生成式改写
- [ ] 完整论文、项目或多图任务已建立来源到图件矩阵；未按章节机械配图
- [ ] 图前合同包含一句核心命题、来源锚点、区域独立贡献、结构原型、载体合同和风险
- [ ] 流程或技术路线已判定 icon 必要性、数量、复杂度、主题映射与配色；未逐节点机械配图
- [ ] PPT 每页已判定主要表达方式，不需要图的页面未强加装饰插图
- [ ] 已检查实际载体、读者、显示尺寸、周围版式和已有视觉系统
- [ ] 视觉简报明确了单一主要功能、主体、风格、构图、配色、材质和负空间
- [ ] 科研属性来自真实内容，无科研符号堆砌、假数据、伪论文或无关仪器
- [ ] 原始分辨率下无伪文字、重复纹理、解剖错误、不合理阴影和生成痕迹
- [ ] 最终显示尺寸下主旨清楚、层级合理、文字可读、裁切稳定、对比充分
- [ ] 精确结构图的文字、数字、节点、箭头、方向和层级与来源逐项一致
- [ ] 流程图使用两类语义主色，必要时才增加第三类；主色相不超过三种，警示色与真实状态一致，未默认套用蓝白模板
- [ ] 图内术语可追溯到来源、方案或方法文献，无内部流程词、生硬直译、自造缩略语或临时拼接短语
- [ ] 全新生成未携带目标图；非编辑参考已转写为拓扑、构图、视觉权重和配色角色
- [ ] 图片修改已携带全部且仅必要的编辑目标，`referenced_image_paths` 与 `num_last_images_to_include` 未并用
- [ ] 载体内嵌图已按语义、位置、文档关系和渲染外观定位，未从导出文件名或媒体序号推断正式图号
- [ ] 项目专属事实只进入本次运行时合同或项目记录，未固化进通用 skill 或 references
- [ ] Image 1 已作为验收基线保留；候选图未在事实、精确内容、结构关系或最终尺寸可读性上降质
- [ ] 携图编辑已填写 `LOCKED / FLEXIBLE / FORBIDDEN`，并与 Image 1 全幅核对
- [ ] Image 2 如存在仅传递配色、间距节奏和线条处理，未传递对象、标签、数据、错误布局或品牌
- [ ] HTTP 524 已与候选图质量失败分开处理；第二次 524 后未继续重试或静默切换模型、API、SVG
- [ ] 未用其它工具修补 imagegen 图内错误文字、数字或箭头
- [ ] 已在最终 PPT、论文页、标书页、报告或网页中渲染检查
- [ ] 网页图已检查响应式裁切、文件体积和替代文本；PPT 已检查缩略图与投影可读性
- [ ] 项目引用的最终图片已进入工作区并使用稳定文件名
- [ ] 必要性与证据、内容与拓扑、视觉与可读性、最终载体四轮签发均已通过
- [ ] 正式项目已记录最终视觉简报、提示词、核验结论和回退原因
